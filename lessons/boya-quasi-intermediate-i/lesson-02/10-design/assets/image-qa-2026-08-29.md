# 第二课图片素材 QA 与用途记录

审核日期：2026-08-29  
课次键：`boya-quasi-intermediate-i:lesson-02`  
依据：`image-manifest.json`、`00-source/canonical-source.json`、来源审核汇总。

## 结果

- **verified：36/36** 图片文件存在、可独立解码，实际 SHA-256 与 `image-manifest.json` 一致。
- **verified：36/36** 图片尺寸为 `1672×941` PNG，符合本课宽幅教材插图规格。
- **verified：用途对应**：29 个词语图、4 个词语理解补充图、3 个短文情境图；每项用途和教材页码已逐条登记在 `image-manifest.json` 的 `use`、`source_refs`、`textbook_printed_pages`。
- **verified：divider**：没有生成 divider，继续复用共用 divider 母版。
- **generated**：全部图片为本项目 OpenAI image_gen 生成并复制到 `10-design/assets/` 的候选素材；不主张外部图片授权。
- **pending**：尚未进入 PPTX；需等本课 visual storyboard、PPTX 内嵌与交付 QA gate。图片正确不等于整课已交付。

## 用途覆盖

| 类别 | 数量 | 用途 |
|---|---:|---|
| vocabulary | 29 | 词语页；其中与词语理解题对应的项目在 manifest 中另列题组用途 |
| vocabulary_comprehension | 4 | 词语理解题的图片辨认／情境支持 |
| short_text_context | 3 | 短文一至三的情境理解与口语任务支持 |

## 核对方法与证据

逐项文件名、尺寸、生成状态、SHA-256、教材页码和用途以同目录 `image-manifest.json` 为准；本次审计重新检查了 manifest 中登记的 36 个文件，未发现缺图、错课次或 hash mismatch。未生成任何新图，也未修改现有图片。

## 待办

1. 在 visual storyboard 中决定实际投影页的选图与版式。
2. 将选用图片内嵌到 PPTX，并检查投影边界与可见性。
3. 由教师完成语义／情境最终确认后，才可随 PPTX 进入批准或 release 包。
