# 第三课图片素材 QA 与用途记录

审核日期：2026-08-29  
课次键：`boya-quasi-intermediate-i:lesson-03`  
依据：`image-manifest.json`、`00-source/canonical-source.json`、来源审核摘要。

## 结果

- **verified：28/28** 图片文件存在、可独立解码，实际 SHA-256 与 `image-manifest.json` 一致；其中 `L03-V24` 德语、`L03-V25` 拉丁语已替换为更直接的语言学习图。
- **verified：23/28** 词语图为 `1254×1254` PNG；**verified：5/28** 情境图／专名图的尺寸与版式记录均在 manifest 中，均为可用 PNG。
- **verified：用途对应**：23 个普通词语图、2 个语言专名图、3 个短文情境图；普通词语图逐张复核后与词义／使用场合相符，语言专名图分别包含可辨认的德语学习材料与拉丁语古典语言材料。每项用途、来源页码和 `source_refs` 已逐条登记在 `image-manifest.json`。
- **verified：用字边界**：普通词语图不嵌入汉字、拉丁字母、数字、logo、水印或气泡；德语／拉丁语专名图只保留少量对应语言文字作为辨识线索；“越来越”只在教材文字层使用，不在图片内生成。
- **verified：divider**：没有生成 divider，继续复用共用 divider 母版。
- **generated**：全部图片为本项目 OpenAI image_gen 生成并复制到 `10-design/assets/` 的候选素材；不主张外部图片授权。
- **pending**：图片已进入本次 `10-design` PPTX draft 重建；仍需通过本课 visual storyboard、PPTX 内嵌与交付 QA gate。图片确认不等于整课已交付。

## 用途覆盖

| 类别 | 数量 | 用途 |
|---|---:|---|
| vocabulary | 23 | 词语页及对应教材听词／短文练习 |
| proper_noun | 2 | “德语”“拉丁语”等语言专名的词语辨识支持 |
| short_text_context | 3 | 短文一至三的情境理解与口语任务支持 |

## 核对方法与证据

逐项文件名、尺寸、生成状态、SHA-256、教材页码和用途以同目录 `image-manifest.json` 为准；本次审计重新检查了 manifest 中登记的 28 个文件，未发现缺图、错课次或 hash mismatch。另为 `德语` 与 `拉丁语` 生成并替换 2 张语义更直接的原创候选图。

## 待办

1. 在 visual storyboard 中决定实际投影页的选图与版式。
2. 将选用图片内嵌到 PPTX，并检查投影边界与可见性。
3. 保持教材原文“越来越”，由教师完成语义／情境最终确认后，才可随 PPTX 进入批准或 release 包。
